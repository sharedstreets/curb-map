"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _assert() {
  const data = _interopRequireDefault(require("assert"));

  _assert = function _assert() {
    return data;
  };

  return data;
}

function _lodash() {
  const data = require("lodash");

  _lodash = function _lodash() {
    return data;
  };

  return data;
}

function _fs() {
  const data = require("fs");

  _fs = function _fs() {
    return data;
  };

  return data;
}

function _path() {
  const data = require("path");

  _path = function _path() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _default() {
  return {
    name: 'theme',

    validate(val) {
      (0, _assert().default)((0, _lodash().isPlainObject)(val) || typeof val === 'string', `The theme config must be Plain Object or String, but got ${val}`);
      const cwd = this.cwd;

      if (typeof val === 'string') {
        const themeFile = (0, _path().isAbsolute)(val) ? val : (0, _path().join)(cwd, val);
        (0, _assert().default)((0, _fs().existsSync)(themeFile), `File ${val} of configure item theme not found.`);
      }
    }

  };
}